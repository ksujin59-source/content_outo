# 주식기록지 연동 설정 가이드 (비개발자용)

주식 기록은 **트렌드 알림 봇과는 별개의 전용 텔레그램 봇**을 씁니다 (`setup_guide.md`의
그 봇과 헷갈리지 마세요). 완료하면:
- 텔레그램에 `매수 삼성전자 10 71000` 처럼 보내면 자동으로 구글시트에 기록됩니다.
- 보유 종목이 전일 종가 대비 ±7% 이상 움직이면 장중 30분 간격으로 텔레그램 알림이 옵니다.

전체 순서:
0. 주식 전용 텔레그램 봇을 새로 만든다
1. 구글시트를 하나 만든다 (주식기록지가 될 시트)
2. 구글 클라우드에서 "서비스 계정"(프로그램이 쓰는 전용 계정)을 만들고 키를 발급받는다
3. 그 서비스 계정을 방금 만든 시트에 "편집자"로 초대한다
4. `.env`와 GitHub Secrets에 값을 채운다
5. 텔레그램으로 매매 기록을 보내 테스트한다

---

## 0단계. 주식 전용 텔레그램 봇 만들기

1. 텔레그램에서 **`BotFather`** 검색 → `/start` → `/newbot` 전송
2. 봇 이름(표시 이름) 입력 — 예: `수진 주식기록봇`
3. 봇 username 입력 — 영문으로, 끝이 반드시 `bot`으로 끝나야 함, 예: `sujin_stock_bot`
4. 발급된 **봇 토큰**(`123456789:AAExxx...` 형태)을 메모장에 복사
5. 텔레그램에서 방금 만든 봇을 검색해 들어가서 아무 메시지나 하나 보내기 (예: "안녕")
6. 브라우저 주소창에 아래 주소 입력 (`<봇토큰>`을 실제 토큰으로 교체):
   ```
   https://api.telegram.org/bot<봇토큰>/getUpdates
   ```
   > ⚠️ 흔한 실수: `<`, `>` 꺾쇠 기호를 안 지우고 그대로 붙여넣거나, `bot`을 토큰 앞에
   > 안 붙이는 경우 `{"ok":false,"error_code":404,...}` 오류가 납니다.
7. 응답에서 **`"chat":{"id":숫자,...}`** 의 숫자가 chat id입니다. **`"update_id"`는
   chat id가 아니니 헷갈리지 마세요** — 둘 다 응답 맨 앞쪽에 비슷하게 나와서 착각하기
   쉽습니다.

---

## 1단계. 구글시트 만들기

1. [sheets.google.com](https://sheets.google.com)에서 새 스프레드시트를 만듭니다.
   이름은 원하는 대로 (예: "주식기록지").
2. 브라우저 주소창의 URL을 봅니다. 아래처럼 생겼습니다.

   ```
   https://docs.google.com/spreadsheets/d/1AbCDefGhijk.......XyZ/edit
   ```

   `/d/`와 `/edit` 사이의 긴 문자열(`1AbCDefGhijk.......XyZ`)이 **시트 ID**입니다.
   메모장에 복사해 두세요. (탭은 나중에 스크립트가 자동으로 만들어 줍니다 —
   미리 만들 필요 없습니다.)

---

## 2단계. 구글 서비스 계정 만들기

1. [Google Cloud Console](https://console.cloud.google.com/)에 접속해서 로그인합니다
   (구글 계정이면 됩니다, 결제 등록 없이 무료로 가능).
2. 새 프로젝트를 하나 만듭니다 (상단 프로젝트 선택 → 새 프로젝트 → 이름은
   "stock-record" 등 자유롭게).
3. 검색창에 **"Google Sheets API"**를 검색해서 들어간 뒤 **사용(Enable)** 버튼을 누릅니다.
4. 왼쪽 메뉴에서 **"API 및 서비스" → "사용자 인증 정보(Credentials)"**로 이동합니다.
5. **"+ 사용자 인증 정보 만들기" → "서비스 계정"**을 선택합니다.
6. 이름을 적당히 입력하고(예: `stock-sheet-bot`) 나머지는 기본값으로 두고 완료합니다.
7. 만들어진 서비스 계정을 클릭 → 상단 **"키(Keys)"** 탭 → **"키 추가" → "새 키 만들기"**
   → 형식은 **JSON** 선택 → 만들기.
8. JSON 파일이 자동으로 다운로드됩니다. **이 파일이 곧 비밀번호입니다.** 절대 깃허브에
   올리거나 다른 사람과 공유하지 마세요. `content-auto` 폴더 **밖**의 안전한 곳
   (예: `C:\Users\ksuji\secrets\`)에 옮겨두세요.
9. 방금 만든 서비스 계정의 **이메일 주소**를 메모장에 복사해 둡니다. 보통
   `stock-sheet-bot@프로젝트이름.iam.gserviceaccount.com` 같은 형태입니다.
   (서비스 계정 목록 페이지에서 확인할 수 있습니다.)

---

## 3단계. 시트에 서비스 계정 초대하기

1. 1단계에서 만든 구글시트로 돌아갑니다.
2. 오른쪽 위 **"공유(Share)"** 버튼을 클릭합니다.
3. 2단계에서 복사한 서비스 계정 이메일을 붙여넣고, 권한을 **"편집자(Editor)"**로
   설정한 뒤 **전송/공유**합니다.
   > ⚠️ 이 단계를 빼먹으면 스크립트가 시트에 접근하지 못하고 오류가 납니다.
   > "권한 없음(PERMISSION_DENIED)" 오류가 나면 대부분 이 단계를 확인하면 됩니다.

---

## 4단계. `.env` 파일에 값 채우기 (로컬 테스트용)

`content-auto/.env` 파일(없으면 `.env.example`을 복사해서 만듭니다)을 열고 아래
네 줄을 채웁니다.

```
GOOGLE_SHEET_ID=1단계에서_복사한_시트_ID
GOOGLE_APPLICATION_CREDENTIALS=2단계에서_다운로드한_JSON_키_파일의_전체_경로
STOCK_TELEGRAM_BOT_TOKEN=0단계에서_받은_주식봇_토큰
STOCK_TELEGRAM_CHAT_ID=0단계에서_확인한_chat_id
```

예:
```
GOOGLE_SHEET_ID=1AbCdefG.......XyZ
GOOGLE_APPLICATION_CREDENTIALS=C:\Users\ksuji\secrets\stock-sheet-bot-key.json
STOCK_TELEGRAM_BOT_TOKEN=123456789:AAExxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
STOCK_TELEGRAM_CHAT_ID=987654321
```

---

## 5단계. 파이썬 패키지 설치

터미널(Git Bash 등)에서 `content-auto` 폴더로 이동한 뒤:

```
pip install -r requirements.txt
```

---

## 6단계. 매매 기록 테스트하기

1. 텔레그램에서 내 봇에게 아래처럼 보냅니다 (종목명 증권사 수량 가격 [메모] 순서 —
   증권사별로 보유수량·평단가를 따로 집계합니다. 메모는 매수 이유 등 자유롭게, 생략 가능).

   ```
   매수 삼성전자 미래에셋 10 71000 반도체 업황 회복 기대
   ```

2. 터미널에서 실행합니다.

   ```
   python scripts/stock/process_trades.py
   ```

3. "새 메시지 없음."이 아니라 처리 로그가 뜨고, 봇에게서
   "✅ 삼성전자(미래에셋) 매수 10주 @71,000 기록 완료" 같은 답장이 오면 성공입니다.
4. 구글시트를 열어서 `거래내역`, `보유종목` 탭이 자동으로 생기고 값이 채워졌는지
   확인합니다.

해외 주식은 티커(영문)로 보내면 됩니다: `매수 AAPL 토스증권 5 190.5`

---

## 7단계. 시세 알람 테스트하기

```
python scripts/stock/check_prices.py
```

장중이 아니거나 변동폭이 ±7% 미만이면 조용히 끝납니다 (로그에 현재 등락률만 출력).
발송 경로 자체를 확인하고 싶으면, 6단계에서 기록한 종목 이름으로 강제 테스트 알람을
보낼 수 있습니다.

```
python scripts/stock/check_prices.py --force-alert 삼성전자
```

이 명령은 실제 알람로그에는 기록하지 않으므로, 이후 정상적인 알람 발송을 막지 않습니다.

---

## 8단계. GitHub Secrets에 등록하기 (자동 실행용)

1. 저장소 페이지 → **Settings → Secrets and variables → Actions**
2. **New repository secret**을 눌러 아래 네 개를 추가합니다.
   - `GOOGLE_SHEET_ID` : 1단계에서 복사한 시트 ID
   - `GOOGLE_SERVICE_ACCOUNT_JSON` : 2단계에서 다운로드한 JSON 키 파일을 **메모장으로 열어
     내용 전체**를 복사해서 붙여넣기 (파일 경로가 아니라 파일 안의 텍스트 전체입니다)
   - `STOCK_TELEGRAM_BOT_TOKEN` : 0단계에서 받은 주식봇 토큰
   - `STOCK_TELEGRAM_CHAT_ID` : 0단계에서 확인한 chat id
   > 기존 `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID`(트렌드 알림 봇)와는 다른 이름이니
   > 헷갈리지 마세요 — 이 기능은 완전히 별도의 봇을 씁니다.
3. **Actions** 탭에서 **"Process Stock Trades"**와 **"Stock Price Alert"** 두
   워크플로우를 각각 열어 **"Run workflow"** 버튼으로 한 번 수동 실행해보고, 초록색
   체크(성공)가 뜨는지 확인합니다.

이후로는:
- 텔레그램에 매매 기록을 보내면 최대 10분 안에 자동으로 시트에 반영됩니다.
- 보유 종목이 장중 ±7% 이상 움직이면 최대 30분 안에 알림이 옵니다.

---

## 문제 해결

**"GOOGLE_SHEET_ID 환경변수가 설정되어 있지 않습니다" 오류**
- `.env` 파일에 `GOOGLE_SHEET_ID` 줄이 있는지, 오타는 없는지 확인하세요.

**"PERMISSION_DENIED" 또는 시트를 못 찾는다는 오류**
- 3단계(시트에 서비스 계정 이메일을 편집자로 공유)를 빼먹지 않았는지 확인하세요.

**"'종목명' 종목을 네이버 검색에서 찾지 못했습니다" 메시지**
- 국내 종목명이 정확한지 확인하세요 (네이버 증권에서 검색되는 정식 종목명 기준).
- 그래도 안 되면 `scripts/stock/kr_ticker_map.json`을 열어
  `"종목명": "종목코드"` 형태로 직접 한 줄 추가하면 됩니다.

**GitHub Actions에서만 실패하고 로컬에서는 잘 됩니다**
- GOOGLE_SERVICE_ACCOUNT_JSON Secret에 JSON 파일의 "내용"이 아니라 "경로"를 잘못
  붙여넣은 경우가 흔합니다. 메모장으로 파일을 열어 `{`로 시작하는 전체 텍스트를
  복사했는지 다시 확인하세요.
