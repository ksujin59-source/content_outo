# 텔레그램 연동 설정 가이드 (비개발자용)

이 문서만 보고 그대로 따라 하면 텔레그램 연동을 완료할 수 있습니다.
컴퓨터 코드를 몰라도 됩니다. 스마트폰과 컴퓨터만 있으면 됩니다.

전체 순서는 이렇습니다:
1. 텔레그램에서 "봇"을 하나 만든다 (봇 = 메시지를 자동으로 보내는 계정)
2. 봇의 "토큰"(비밀번호 같은 것)을 받는다
3. 메시지를 받을 내 채팅방의 "chat id"(번호표 같은 것)를 알아낸다
4. 이 두 값을 `.env` 파일에 적어 넣는다
5. 잘 작동하는지 테스트한다

---

## 1단계. 텔레그램 봇 만들기

1. 스마트폰이나 PC에서 **텔레그램(Telegram)** 앱을 엽니다. (설치가 안 되어 있다면
   앱스토어/플레이스토어에서 "Telegram" 검색 후 설치, 전화번호로 가입)
2. 텔레그램 검색창에 **`BotFather`** 를 검색합니다. 파란 체크 표시가 있는 공식 계정을
   선택하세요. (주소: `@BotFather`)
3. BotFather와의 채팅방에 들어가서 **`/start`** 를 입력하고 전송합니다.
4. 이어서 **`/newbot`** 을 입력하고 전송합니다.
5. BotFather가 "봇 이름을 정해달라"고 물어봅니다. 원하는 이름을 입력합니다.
   예: `수진의 AI 트렌드 봇`
6. 이어서 "봇의 username을 정해달라"고 물어봅니다. username은 **반드시 영문으로 끝이
   `bot`으로 끝나야** 합니다.
   예: `sujin_ai_trend_bot`
7. 성공하면 아래와 비슷한 메시지를 받습니다.

   ```
   Done! Congratulations on your new bot. ...
   Use this token to access the HTTP API:
   123456789:AAExxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```

   **이 긴 문자열(`123456789:AAExxx...`)이 바로 "봇 토큰"입니다.**
   이건 이 봇을 조종할 수 있는 비밀번호와 같습니다. 다른 사람에게 절대 공유하지 마세요.
   메모장 같은 곳에 잠깐 복사해 두세요. (나중에 3단계 파일에 붙여넣을 것입니다)

---

## 2단계. 내 봇과 대화 시작하기

봇은 사용자가 먼저 말을 걸기 전까지는 그 사람에게 메시지를 보낼 수 없습니다.
그래서 딱 한 번, 내가 먼저 봇에게 말을 걸어줘야 합니다.

1. 텔레그램 검색창에서 방금 만든 봇의 username(예: `sujin_ai_trend_bot`)을 검색합니다.
2. 봇 채팅방에 들어가서 **아무 메시지나 하나 보냅니다.** (예: "안녕")
   - 봇이 답장을 안 해도 정상입니다. 지금은 "내가 이 봇에게 말을 걸었다"는 기록만
     남기면 됩니다.

---

## 3단계. chat id 알아내기

이제 "이 메시지를 어느 채팅방으로 보낼지" 알려주는 번호(chat id)를 찾아야 합니다.
아래 두 방법 중 편한 것을 하나만 하면 됩니다.

### 방법 A. 브라우저 주소창으로 확인하기 (가장 쉬움, 추천)

1. 컴퓨터나 스마트폰의 인터넷 브라우저(크롬 등)를 엽니다.
2. 주소창에 아래 주소를 입력하되, `<봇토큰>` 부분을 1단계에서 받은 실제 토큰으로
   바꿔서 입력합니다.

   ```
   https://api.telegram.org/bot<봇토큰>/getUpdates
   ```

   예시 (실제 토큰을 그대로 붙여넣으면 됩니다):
   ```
   https://api.telegram.org/bot123456789:AAExxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx/getUpdates
   ```

3. 화면에 아래와 비슷한 텍스트가 나타납니다. 그 안에서 **`"chat":{"id":123456789,...`**
   부분을 찾으세요. `id` 뒤에 있는 숫자가 여러분의 **chat id**입니다.

   ```
   {"ok":true,"result":[{"update_id":...,
     "message":{"message_id":1,"from":{...},
       "chat":{"id":987654321,"first_name":"수진","type":"private"},
       "date":...,"text":"안녕"}}]}
   ```

   위 예시에서는 `987654321` 이 chat id입니다.

   > 만약 화면에 `"result":[]` 처럼 텅 비어 있다면, 2단계에서 봇에게 메시지를 보내지
   > 않았거나, 아직 반영이 안 된 것입니다. 봇 채팅방에 메시지를 하나 더 보낸 뒤
   > 브라우저 새로고침을 해보세요.

### 방법 B. 다른 봇의 도움 받기

1. 텔레그램에서 **`@userinfobot`** 을 검색해 채팅을 시작합니다.
2. 자동으로 내 계정 정보와 함께 **Id: 숫자** 형태로 chat id를 알려줍니다.

---

## 4단계. `.env` 파일에 값 채워 넣기

1. 이 프로젝트 폴더(`content-auto`)를 엽니다.
2. `.env.example` 파일을 복사해서, 복사본 이름을 **`.env`** (점으로 시작, 확장자 없음)
   로 바꿉니다.
   - 탐색기에서 파일을 복사(Ctrl+C) → 붙여넣기(Ctrl+V) → 이름 바꾸기(F2) → `.env` 입력
3. `.env` 파일을 메모장으로 엽니다 (파일 우클릭 → 연결 프로그램 → 메모장).
4. 아래처럼 두 줄을 실제 값으로 채웁니다.

   ```
   TELEGRAM_BOT_TOKEN=1단계에서_받은_긴_토큰_문자열
   TELEGRAM_CHAT_ID=3단계에서_찾은_숫자
   ```

   예:
   ```
   TELEGRAM_BOT_TOKEN=123456789:AAExxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   TELEGRAM_CHAT_ID=987654321
   ```

5. 저장하고 닫습니다.

   > ⚠️ `.env` 파일은 절대 다른 사람과 공유하거나 인터넷에 올리지 마세요.
   > 이 파일은 자동으로 git에는 올라가지 않도록 설정되어 있습니다(`.gitignore`).

---

## 5단계. 잘 작동하는지 테스트하기

### 터미널을 쓸 수 있다면 (권장)
프로젝트 폴더에서 터미널(Git Bash 등)을 열고 아래 명령을 실행합니다.

```
./scripts/send_telegram.sh "테스트 메시지입니다"
```

`텔레그램 전송 성공` 이라는 문구가 뜨고, 텔레그램 봇 채팅방에 "테스트 메시지입니다"가
도착했다면 성공입니다.

### 터미널이 낯설다면 (브라우저만으로 테스트)
아래 주소를 브라우저 주소창에 입력하되, `<봇토큰>`과 `<chat id>`를 실제 값으로
바꿔서 입력합니다.

```
https://api.telegram.org/bot<봇토큰>/sendMessage?chat_id=<chat id>&text=테스트메시지
```

엔터를 누른 뒤 텔레그램 봇 채팅방을 확인해서 "테스트메시지"가 도착했으면 성공입니다.

---

## 6단계. 클라우드 자동화(매일 아침 7시 실행) 연결

로컬 `.env` 파일은 내 컴퓨터에서 직접 테스트할 때만 쓰입니다. 매일 아침 7시에
자동으로 실행되는 부분은 **클라우드**(Anthropic 서버)에서 돌아가기 때문에, 내 컴퓨터의
`.env` 파일을 읽을 수 없습니다. 그래서 위에서 확인한 **봇 토큰**과 **chat id** 두 값을
클라우드 라우틴 설정에도 한 번 더 등록해야 합니다 — 이 부분은 Claude가 라우틴을 만들 때
같이 처리해줍니다. (`https://claude.ai/code/routines` 에서 라우틴 목록과 실행 기록을
확인할 수 있습니다.)

라우틴이 잘 실행되고 있는지 확인하려면:
1. `https://claude.ai/code/routines` 접속
2. 라우틴 이름을 클릭 → 실행 기록(run) 목록 확인
3. 매일 아침 텔레그램으로 메시지가 오는지, `content/blog` 폴더에 새 파일이 쌓이는지 확인

---

## 문제 해결

**"텔레그램 전송 실패"라고 뜨거나 메시지가 안 옵니다.**
- 2단계(봇에게 먼저 말 걸기)를 빼먹지 않았는지 확인하세요. 이게 제일 흔한 원인입니다.
- `.env`의 토큰/chat id에 공백이나 줄바꿈이 잘못 들어가지 않았는지 확인하세요.
- 봇 토큰이 맞는지 BotFather 채팅방에서 `/mybots` → 봇 선택 → `API Token` 으로
  다시 확인할 수 있습니다.

**chat id를 못 찾겠어요 (`"result":[]`로만 나옵니다).**
- 봇 채팅방에 메시지를 다시 한번 보낸 뒤, 브라우저 주소를 새로고침 해보세요.
- 봇이 아니라 "일반 사람"과의 채팅에 메시지를 보낸 건 아닌지 확인하세요 (반드시 내가
  만든 봇의 채팅방이어야 합니다).

**여러 사람이 보는 채널이나 그룹으로 보내고 싶어요.**
- 채널/그룹을 만들고, 그 채널/그룹에 내 봇을 관리자로 추가합니다.
- 채널/그룹에서 아무 메시지나 보낸 뒤, 1단계와 동일하게 `getUpdates` 주소로 확인하면
  chat id가 `-100`으로 시작하는 음수 값으로 나옵니다. 이 값을 그대로 사용하면 됩니다.
