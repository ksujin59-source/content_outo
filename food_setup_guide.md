# 이유식 기록 연동 설정 가이드 (비개발자용)

이유식 기록은 **새 봇을 만들지 않고, 이미 쓰고 있는 주식 기록 봇을 그대로 재사용**합니다.
완료하면:
- 주식봇에게 `이유식 고구마, 소고기`처럼 "이유식" 접두어를 붙여서 보내면 자동으로:
  - 구글 캘린더에 그날 하루 이벤트 하나로 계속 누적되어 기록되고,
  - 구글시트 `이유식기록` 탭에도 한 줄씩 쌓입니다 (알레르기 확인용 이력).
- 같은 날 여러 번 보내도 캘린더 이벤트는 새로 생기지 않고 기존 이벤트에 줄만 추가됩니다.
- 접두어 없이 보내는 기존 매매 메시지(`매수 삼성전자 ...`)는 지금처럼 그대로 작동합니다 —
  "이유식"으로 시작하는 메시지만 이유식 기록으로 처리되고, 나머지는 기존 로직 그대로입니다.

**주식기록지 설정(`stock_setup_guide.md`)이 이미 되어 있다는 전제**입니다. 봇 토큰,
구글시트, 서비스 계정을 전부 재사용하므로 새로 만들 건 **구글 캘린더 하나**뿐입니다.

전체 순서:
1. 구글 클라우드에서 Calendar API를 활성화한다 (Sheets API는 이미 켜져 있음)
2. 이유식 기록 전용 구글 캘린더를 새로 만든다 (주 캘린더에 섞지 않음)
3. 그 캘린더를 서비스 계정과 공유한다 (주식기록지에 쓰던 그 서비스 계정)
4. `.env`와 GitHub Secrets에 캘린더 ID 하나만 추가한다
5. 텔레그램으로 테스트한다

---

## 1단계. Google Calendar API 활성화하기

주식기록지 설정 때 만든 것과 **같은 구글 클라우드 프로젝트**를 그대로 씁니다.

1. [Google Cloud Console](https://console.cloud.google.com/)에서 그 프로젝트를 선택합니다.
2. 검색창에 **"Google Calendar API"**를 검색해서 들어간 뒤 **사용(Enable)** 버튼을 누릅니다.
   (Google Sheets API를 켰던 것과 같은 화면, API 이름만 다릅니다.)

> 이 단계를 빼먹으면 나중에 `PERMISSION_DENIED` 또는 "Calendar API has not been used in
> project ... before it is disabled" 같은 오류가 납니다.

---

## 2단계. 이유식 기록 전용 구글 캘린더 만들기

1. [calendar.google.com](https://calendar.google.com)에 접속합니다.
2. 왼쪽 "다른 캘린더" 옆 **+** 버튼 → **"새 캘린더 만들기"**
3. 이름을 입력합니다 — 예: `이유식 기록`. **주 캘린더(내 이름으로 된 기본 캘린더)가
   아니라 이 새 캘린더**를 씁니다.
4. **"캘린더 만들기"** 클릭.

---

## 3단계. 캘린더를 서비스 계정과 공유하기

주식기록지 설정 때 만든 **서비스 계정 이메일**(`...iam.gserviceaccount.com`로 끝나는
주소, `stock_setup_guide.md` 2단계에서 메모해둔 것)을 그대로 씁니다. 새 서비스 계정을
또 만들 필요는 없습니다.

1. 왼쪽 "다른 캘린더" 목록에서 방금 만든 캘린더에 마우스를 올리고 **⋮ → 설정 및 공유**로
   들어갑니다.
2. **"특정 사용자와 공유"** → **"사용자 추가"**
3. 서비스 계정 이메일을 붙여넣고, 권한을 반드시 **"일정 변경"**으로 설정합니다
   (기본값인 "모든 일정 세부정보 보기"는 읽기 전용이라 안 됩니다 — 기록을 써야 하므로).
   > ⚠️ 이 단계를 빼먹으면 스크립트가 캘린더에 접근하지 못하고 `PERMISSION_DENIED`
   > 오류가 납니다.
4. 같은 설정 화면의 **"캘린더 통합"** 섹션까지 아래로 스크롤해서 **"캘린더 ID"**를
   복사해둡니다. `xxxxxxxxxxxxxxxx@group.calendar.google.com` 같은 형태입니다.
   > ⚠️ 캘린더 이름("이유식 기록")이나 브라우저 주소창 URL이 아니라, 이 "캘린더 ID"
   > 필드에 있는 값이어야 합니다.

---

## 4단계. `.env` 파일에 값 채우기 (로컬 테스트용)

`content-auto/.env` 파일을 열고 아래 한 줄만 새로 채웁니다 (나머지 값들은 주식기록지
설정 때 이미 채워져 있으므로 그대로 둡니다).

```
FOOD_GOOGLE_CALENDAR_ID=3단계에서_복사한_캘린더_ID
```

예:
```
FOOD_GOOGLE_CALENDAR_ID=abcdef1234567890@group.calendar.google.com
```

---

## 5단계. 이유식 기록 테스트하기

1. 텔레그램에서 **기존 주식봇**에게 "이유식" 접두어를 붙여서 오늘 먹은 음식을 보냅니다.
   쉼표로 여러 개를 구분할 수 있습니다.

   ```
   이유식 고구마, 소고기
   ```

2. 터미널에서 실행합니다 (주식기록 테스트할 때와 같은 명령입니다).

   ```
   python scripts/stock/process_trades.py
   ```

3. 봇에게서 "✅ 2026-09-17 이유식 기록 완료: 고구마, 소고기" 같은 답장이 오면
   성공입니다.
4. 구글시트를 열어서 `이유식기록` 탭이 자동으로 생기고 방금 보낸 음식이 한 줄씩
   (음식 개수만큼) 채워졌는지 확인합니다.
5. 구글 캘린더(2단계에서 만든 캘린더)를 열어서 오늘 날짜에 "이유식 기록 - YYYY-MM-DD"
   종일 이벤트가 생겼고, 설명에 방금 보낸 내용이 적혀 있는지 확인합니다.
6. 같은 날 다시 한 번 "이유식 당근"을 보내고 스크립트를 재실행해서, 캘린더에
   **새 이벤트가 또 생기는 게 아니라** 기존 이벤트 설명에 줄이 추가되는지 확인합니다.
7. "이유식" 접두어 없이 기존처럼 `매수 삼성전자 미래에셋 10 71000`을 보내고 재실행해서,
   주식 기록도 평소처럼 잘 되는지 확인합니다 (이유식 기능이 기존 기능을 안 건드렸는지
   확인하는 절차).

---

## 6단계. GitHub Secrets에 등록하기 (자동 실행용)

1. 저장소 페이지 → **Settings → Secrets and variables → Actions**
2. **New repository secret**을 눌러 아래 한 개를 추가합니다 (나머지 시크릿은 주식기록지
   설정 때 이미 등록되어 있으므로 새로 추가할 필요 없습니다).
   - `FOOD_GOOGLE_CALENDAR_ID` : 3단계에서 복사한 캘린더 ID
3. **Actions** 탭에서 **"Process Stock Trades"** 워크플로우를 열어 **"Run workflow"**
   버튼으로 한 번 수동 실행해보고, 초록색 체크(성공)가 뜨는지 확인합니다.

이후로는 주식봇에 "이유식 ..."으로 시작하는 메시지를 보내면 최대 10분 안에 캘린더와
시트에 자동으로 반영됩니다.

---

## 문제 해결

**"FOOD_GOOGLE_CALENDAR_ID 환경변수가 설정되어 있지 않습니다" 오류**
- `.env` 파일에 `FOOD_GOOGLE_CALENDAR_ID` 줄이 있는지, 오타는 없는지 확인하세요.

**"PERMISSION_DENIED" 또는 캘린더를 못 찾는다는 오류**
- 1단계(Calendar API 활성화)와 3단계(캘린더를 서비스 계정과 "일정 변경" 권한으로
  공유)를 다시 확인하세요. "모든 일정 세부정보 보기" 권한으로만 공유하면 읽기는
  되지만 쓰기(이벤트 생성/수정)는 안 됩니다.

**캘린더 ID를 넣었는데도 계속 오류**
- 캘린더 이름이나 URL이 아니라, 캘린더 설정 화면의 "캘린더 통합" 섹션에 있는
  "캘린더 ID" 필드 값인지 다시 확인하세요 (`...@group.calendar.google.com` 형태).

**"이유식 고구마"라고 보냈는데 "형식을 인식하지 못했습니다" 답장이 옵니다**
- 접두어와 음식 사이에 띄어쓰기가 있는지 확인하세요 (`이유식고구마`처럼 붙여 쓰면
  인식되지 않습니다).

**GitHub Actions에서만 실패하고 로컬에서는 잘 됩니다**
- `GOOGLE_SERVICE_ACCOUNT_JSON` Secret이 이미 등록돼 있는지 확인하세요 (주식기록지
  설정 때 등록한 것을 그대로 씁니다).

**같은 날 여러 번 보냈는데 캘린더 이벤트가 여러 개 생겼습니다**
- 정상적으로는 생기지 않도록 설계되어 있습니다 (날짜별로 고정된 이벤트 ID를 씀).
  이런 일이 보이면 `FOOD_GOOGLE_CALENDAR_ID`가 중간에 바뀌지 않았는지, 또는 캘린더
  앱에서 수동으로 이벤트를 복제하지 않았는지 확인하세요.
